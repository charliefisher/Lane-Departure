runtime_editable: false,  # these settings are read-only at runtime

gaussian_blur: {
    kernel_size: '(15, 15)',
    deviation: 0
},
sliding_window: {
    height: 5,
    n_max_per_window: 2,
    active_threshold: 100
}
